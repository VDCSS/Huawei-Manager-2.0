
# Dependências principais
python-dotenv>=1.0.0
cryptography>=42.0.0
netmiko>=4.0.0

# PyYAML — necessário para SopsBackend (SECRETS_BACKEND=sops)
pyyaml>=6.0

# ── Backends de secrets opcionais (Fase 3) ────────────────────────────
# Descomente conforme o ambiente:
# hvac>=1.2.0          # HashiCorp Vault
# boto3>=1.34.0        # AWS Secrets Manager
# sops CLI + age       # Instalar externamente: https://github.com/getsops/sops
