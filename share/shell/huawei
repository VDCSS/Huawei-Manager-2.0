#!/usr/bin/env bash
case "${1:-}" in
    manager)
        shift
        exec __VENV_DIR__/bin/huawei-manager "$@"
        ;;
    *)
        echo "Huawei Manager — CLI"
        echo ""
        echo "Subcomandos:"
        echo "  manager    Abre a interface grafica"
        ;;
esac
